// App logic
// define all necessaries libraries includes into the theme
define('main', ["jquery",
    "bootstrap",
    "tools",
    "tiny-scrollbar"], function($) {
    //the jquery.alpha.js and jquery.beta.js plugins have been loaded.

    $(function() {
        // Initiation of themes's functions when dom is ready
    });
});
